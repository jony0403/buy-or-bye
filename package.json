{
  "name": "bunjang-local-images",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.mjs",
    "install-browser": "npx playwright install chromium"
  },
  "dependencies": {
    "playwright": "^1.50.0"
  }
}
